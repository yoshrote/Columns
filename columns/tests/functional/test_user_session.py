from columns.tests import *

class TestUserSessionController(TestController):
	pass
	'''
	def test_create(self):
		response = self.app.get(url(controller='user_session', action='create'))
		# Test response...
	
	def test_delete(self):
		response = self.app.get(url(controller='user_session', action='delete'))
		# Test response...
	
	def test_challenge(self):
		response = self.app.get(url(controller='user_session', action='challenge'))
		# Test response...
	
	def test_verify(self):
		response = self.app.get(url(controller='user_session', action='verify'))
		# Test response...
	
	def test_xrds(self):
		response = self.app.get(url(controller='user_session', action='xrds'))
		# Test response...
	
'''